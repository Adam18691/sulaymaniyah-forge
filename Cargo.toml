[package]
name = "sulaymaniyah_forge"
version = "0.1.0"
edition = "2021"

[dependencies]
# مكتبة الاتصال بالـ API
reqwest = { version = "0.11", features = ["blocking", "json"] }
# مكتبات تحويل البيانات من وإلى JSON
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
